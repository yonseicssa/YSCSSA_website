# YSCSSA_website
YSCSSA_website

## React 开发方式
先安装依赖：

```bash
npm install
```

启动开发服务器：

```bash
npm run dev
```

然后浏览器打开命令行提示的本地地址（默认通常是 http://localhost:5173）

## 生产构建

```bash
npm run build
npm run preview
```

`npm run build` 会在 `dist` 目录生成静态文件，`npm run preview` 用于本地预览生产版本。



## ToDo:
### footer处 调整联系我们
* 微信，ins，email的logo
* 各个联系方式所对应的跳转位置
### 关于我会页面中
* 设置背景图片 (?)
* 调整页面布局
    * 将关于我们的图片改为流动式的图片
    * 将我们的服务改为可点击并在点击后显示详情的样式
        * 在左边列出一列
        * 点击后右侧给出详情
### 部门构成页面中
* 将组织架构改为可点击并在点击后显示详情的样式
    * 在左边列出一列
    * 点击后右侧给出详情
        * 有部门介绍为顶端文字
        * 以流动式展示部门成员
* 加入我们模块更改
    * 立即报名转到招聘公告位置
### 活动概览页面中
（要不要做这么麻烦，还是按照学校官网的公告样式）
* 页面布局更改（现在因为调整页面宽度导致各个模块移位）
* 精彩活动和精彩瞬间改为： 即将进行和往期活动
* 在即将进行和往期活动中设置json文件保存宣传图片路径，活动标题，活动详情，活动位置，活动时间，详细信息。 以此来设置成卡片展览
    * 详细信息：点击后跳转点击活动卡片后跳转到相应宣传位置（微信公众号，小红书等）
* 往期活动展示方式更改
    * 在左边列出一列
        * 点击将卡片扩大
            * 原先只显示标题
            * 点击后显示完整卡片，再添加一个详细信息，点击后跳转
            * 默认先显示第一个
    * 右侧以流动方式展示往期活动的图片内容
